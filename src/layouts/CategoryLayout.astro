---
import Layout from './Layout.astro'
export interface Props {
  title: string
  heading?: string
}

const { title, heading } = Astro.props
---

<Layout title={title}>
  {heading && <h1 class="mt-2 text-2xl">{heading}</h1>}
  <slot />
</Layout>
