---
import Layout from './Layout.astro'
export interface Props {
  title: string
  heading: string
}

const { title, heading } = Astro.props
---

<Layout title={title}>
  <h1 class="mb-4 text-2xl">{heading}</h1>
  <slot />
</Layout>
